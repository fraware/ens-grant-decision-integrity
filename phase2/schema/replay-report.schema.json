{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "replay-report.schema.json",
  "title": "ENS GDI Phase II Replay Report",
  "description": "Per-layer replay outcomes. Agreement is not correctness, fairness, or legitimacy.",
  "type": "object",
  "additionalProperties": false,
  "required": ["reportVersion", "manifestCommitmentDigest", "layers"],
  "properties": {
    "reportVersion": { "const": "1" },
    "manifestCommitmentDigest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "layers": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["layerId", "outcome"],
        "properties": {
          "layerId": {
            "type": "string",
            "enum": [
              "preprocessing",
              "retrieval-snapshot",
              "scoring",
              "aggregation",
              "hosted-generation"
            ]
          },
          "outcome": {
            "type": "string",
            "enum": ["exact-match", "bounded-match", "diverged", "not-replayable"]
          },
          "attestedDigest": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          },
          "recomputedDigest": {
            "type": ["string", "null"],
            "pattern": "^[0-9a-f]{64}$"
          },
          "bound": { "type": ["string", "null"] },
          "reason": { "type": ["string", "null"] }
        },
        "allOf": [
          {
            "if": {
              "properties": { "outcome": { "const": "not-replayable" } },
              "required": ["outcome"]
            },
            "then": {
              "properties": { "reason": { "type": "string", "minLength": 1 } },
              "required": ["reason"]
            }
          },
          {
            "if": {
              "properties": { "outcome": { "const": "bounded-match" } },
              "required": ["outcome"]
            },
            "then": {
              "properties": { "bound": { "type": "string", "minLength": 1 } },
              "required": ["bound"]
            }
          }
        ]
      }
    }
  }
}
